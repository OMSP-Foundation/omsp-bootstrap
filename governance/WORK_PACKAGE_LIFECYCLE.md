# Work Package Lifecycle

A Work Package is the smallest governed unit of OMSP engineering work.

Lifecycle:

```text
Backlog → Issue → Branch → Commit → Draft PR → Review → Merge → Baseline Update
```

Every Work Package has a unique WP identifier and must preserve traceability to its issue and pull request.
