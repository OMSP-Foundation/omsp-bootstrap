# Contribution Workflow

All OMSP contributions follow the standard sequence:

Issue → Branch → Commit → Draft PR → Review → Merge.

No direct commits to `develop` or `main` are allowed for normal work.
