# Release Policy

OMSP releases follow Semantic Versioning.

Release types:

- bootstrap
- alpha
- beta
- baseline
- stable

A release requires a clean `develop` state, reviewed work packages, and explicit baseline approval.
